# Browser Type (default browser, can be overridden via TestNG or Maven)
browser=Chrome

# Base URL of the application under test
baseUrl=https://dsportalapp.herokuapp.com

# Maximize browser window by default (set to true or false)
maximizeWindow=true